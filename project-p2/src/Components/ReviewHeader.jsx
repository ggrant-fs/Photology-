import React from 'react';

const ReviewHeader = () => {
    return (
        <div>
            <header id="review-header" className="header-template"></header>
        </div>
    );
};

export default ReviewHeader;